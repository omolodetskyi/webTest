package pageObjects;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class NewWindow extends H3TitlePage {
	
	public NewWindow(WebDriver driver) {
		super(driver);
	}
    
}
